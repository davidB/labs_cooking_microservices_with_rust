# reviews - final

```
curl http://localhost/reviews/0

curl http://localhost/reviews/0 -H 'Content-Type: application/json' -X POST -d '{"reviewer": "my_name", "rating": 3, "text": "my review"}'
curl http://localhost/reviews/0
```
